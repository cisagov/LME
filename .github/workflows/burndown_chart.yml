name: Burndown Chart 

on: 
  workflow_dispatch:
  pull_request:
    branches:
      - '*'

jobs:
  my_job:
    runs-on: ubuntu-latest
    
    steps:
    - name: Print a greeting 
      run: echo "Hello, world!"