# Add host IP address / domain names as needed.

instances:
  - name: "elasticsearch"
    dns:
      - "lme-elasticsearch"
      - "localhost"
    ip:
      - "127.0.0.1"

  - name: "kibana"
    dns:
      - "lme-kibana"
      - "localhost"
    ip:
      - "127.0.0.1"

  - name: "fleet-server"
    dns:
      - "lme-fleet-server"
      - "localhost"
    ip:
      - "127.0.0.1"

  - name: "wazuh-manager"
    dns:
      - "lme-wazuh-manager"
      - "localhost"
    ip:
      - "127.0.0.1"

  - name: "logstash"
    dns:
      - "logstash"
      - "localhost"
    ip:
      - "127.0.0.1"

  - name: "curator"
    dns:
      - "curator"
      - "localhost"
    ip:
      - "127.0.0.1"

  - name: "fleet-distribution"
    dns:
      - "lme-fleet-distribution"
      - "localhost"
    ip:
      - "127.0.0.1"

  - name: "litellm"
    dns:
      - "lme-litellm"
      - "localhost"
    ip:
      - "127.0.0.1"

  - name: "dashboard"
    dns:
      - "lme-dashboard"
      - "localhost"
    ip:
      - "127.0.0.1"

  - name: "log-analyzer"
    dns:
      - "lme-log-analyzer"
      - "localhost"
    ip:
      - "127.0.0.1"

  - name: "embeddings"
    dns:
      - "lme-embeddings"
      - "localhost"
    ip:
      - "127.0.0.1"

  - name: "llama-cpp"
    dns:
      - "lme-llama-cpp"
      - "localhost"
    ip:
      - "127.0.0.1"
