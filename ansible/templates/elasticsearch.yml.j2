cluster.name: "docker-cluster"
network.host: 0.0.0.0
xpack.ml.enabled: false
path:
  repo:
    - /usr/share/elasticsearch/backups
{% if lme_cluster_mode | default(false) %}
    - /usr/share/elasticsearch/snapshots
{% endif %}

