---
- name: Restart wazuh manager
  ansible.builtin.command: >-
    podman exec lme-wazuh-manager
    /var/ossec/bin/wazuh-control restart
  become: true
  changed_when: true

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true
