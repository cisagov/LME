# Testing Notes

## causand22 det eng notes
- Detection engineering sub-tab does not match height of alerts sub-tab. also does not have blue bar indicating which tab
- using cdn tailwind for site. fine for demo but should switch to non-cdn.
- detection engineering / sigma rules
  - click 'upload to kibana' for windows, then 'upload to kibana' for macos -- the spinner stops on upload to windows. Has the upload stopped?
  - after successful upload the 'upload to kibana' button does not disappear or grey out


*Improvements and upgrades to make*:
- make styling consistent on detection engineering page
- back button works (might be hard to do without re-writing entire app in a better way e.g. react)

